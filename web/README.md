# web/

Single-page dashboard: one card per machine (current state, duration, last
cycle length, auto-refresh). Reads from `GET /machines`.

Not scaffolded yet -- initialize when you get here:

```
npm create vite@latest . -- --template react
npm install
npm install -D tailwindcss postcss autoprefixer
```
